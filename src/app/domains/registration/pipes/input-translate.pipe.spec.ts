import { InputTranslatePipe } from './input-translate.pipe';

describe('InputTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new InputTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
